diff --git a/src/webservice.cpp b/src/webservice.cpp
index f27fe11..a60ee5e 100644
--- a/src/webservice.cpp
+++ b/src/webservice.cpp
@@ -12013,6 +12013,8 @@ void handle_about(AsyncWebServerRequest *request)
 	strcat(webString, "- New: TCP KISS Server (MOD tab) lets PC software (Xastir, APRSIS32, etc) use this device as a network TNC over WiFi, 2 ports, no cable needed. Off by default<br />\n");
 strcat(webString, "- Fixed a buffer overflow risk in the IGATE/DIGI PHG Text field (unbounded strcpy on an 8-byte buffer)<br />\n");
 strcat(webString, "- Simplified OTA Online Firmware Update: now only checks and links to the new version, no more auto-download/auto-flash. Use Manual Firmware Update to install<br />\n");
+strcat(webString, "- Manual Filesystem Update now auto-restores your WiFi/APRS config after writing icons, so it survives even without USB access<br />\n");
+strcat(webString, "- Fixed a bug where submitting Manual Filesystem Update also fired a stray request from Manual Firmware Update, breaking the icons write<br />\n");
 	strcat(webString, "</td></tr>\n");
 	strcat(webString, "</table><br />\n");
 
@@ -12190,7 +12192,7 @@ strcat(webString, "- Simplified OTA Online Firmware Update: now only checks and
 					  "var fileName = obj.value.split('\\\\');"
 					  "document.getElementById('file-input').innerHTML = '   '+ fileName[fileName.length-1];"
 					  "};"
-					  "$('form').submit(function(e){"
+					  "$('#upload_form').submit(function(e){"
 					  "e.preventDefault();"
 					  "var form = $('#upload_form')[0];"
 					  "var data = new FormData(form);"
@@ -12277,7 +12279,7 @@ strcat(webString, "- Simplified OTA Online Firmware Update: now only checks and
 			"<tr><td align=\"right\"><b>Current Version:</b></td><td align=\"left\">V%s%s</td></tr>\n",
 			VERSION, VERSION_BUILD);
 		strcat(webString, ota_row);
-		strcat(webString, "<tr><td align=\"right\"><b>Status:</b></td><td><span id='ota_prg'>Click 'Check New Version' to check for updates. To install, download the file above and use Manual Firmware Update.</span></td></tr>\n");
+		strcat(webString, "<tr><td align=\"right\"><b>Status:</b></td><td><span id='ota_prg'>Ready</span></td></tr>\n");
 		strcat(webString, "<tr><td colspan=\"2\" align=\"right\"><div class=\"col-sm-3 col-xs-4\"><input type='button' class=\"btn btn-danger\" id=\"check_ver_btn\" value='Check New Version'></div></td></tr>\n");
 		strcat(webString, "</table><br />\n");
 	}
@@ -12297,7 +12299,7 @@ strcat(webString, "- Simplified OTA Online Firmware Update: now only checks and
 					  "var fwLink = document.getElementById('fw_file_link');"
 					  "fwLink.href = newUrl;"
 					  "fwLink.innerHTML = d.latest_filename;"
-					  "document.getElementById('ota_prg').innerHTML = 'New version available: V' + d.latest_version + d.latest_build + ' (' + d.latest_date + ') - click the link above to download, then use Manual Firmware Update below.';"
+					  "document.getElementById('ota_prg').innerHTML = 'New version available: V' + d.latest_version + d.latest_build + '. Download link above, install via Manual Firmware Update.';"
 					  "} else {"
 					  "document.getElementById('ota_prg').innerHTML = 'You are using the latest version.';"
 					  "}"
